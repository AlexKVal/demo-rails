<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name do %>Your name <%= f.text_field :name %><% end %>
  <br>
  <%= f.label :email do %>Your email <%= f.email_field :email %><% end %>

  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
