<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title><%= title %> | <%= Rails.configuration.app_name %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag    'test-overrides' if Rails.env.test? %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= render 'shim' %>
  </head>

  <body>
    <%= render 'header' %>
    <div class="container">
      <%= render 'flash' %>
      <%= yield %>
      <%= render 'footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>

    <%= javascript_include_tag 'cljs-admin' %>
  </body>
</html>
