// TODO update Followers page via .js.erb
// this should somehow update avatars_block and Followers list on the followers' pages
$('#follow_unfollow').html("<%= escape_javascript(render('users/follow_unfollow_button')) %>")
$('#followers').html('<%= @user.followers.count %>')
