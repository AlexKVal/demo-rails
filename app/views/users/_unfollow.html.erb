<%= form_for current_user.active_relationships.find_by(followed: @user),
             method: :delete, html: {class: 'wide-inputs'}, remote: true do |f| %>
  <%= f.submit 'Unfollow', class: 'btn', name: nil %>
<% end %>
